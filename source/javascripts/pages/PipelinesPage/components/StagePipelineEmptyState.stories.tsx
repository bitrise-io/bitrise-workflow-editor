import { Meta, StoryObj } from '@storybook/react';
import StagePipelineEmptyState from './StagePipelineEmptyState'; /// Meta and stories for StagePipelineEmptyState

/// Meta and stories for StagePipelineEmptyState
export default {
  component: StagePipelineEmptyState,
} as Meta<typeof StagePipelineEmptyState>;

export const StagePipeline: StoryObj<typeof StagePipelineEmptyState> = {};
