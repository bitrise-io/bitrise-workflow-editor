import { Meta, StoryObj } from '@storybook/react';
import AddTriggerDialog from './AddTriggerDialog';

export default {
  component: AddTriggerDialog,
} as Meta<typeof AddTriggerDialog>;

export const Default: StoryObj<typeof AddTriggerDialog> = {};
