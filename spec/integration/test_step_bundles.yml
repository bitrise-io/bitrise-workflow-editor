step_bundles:
  included-bundle:
    title: Included step bundle
    summary: A step bundle from an included file
    steps:
      - script@1:
          title: Bundle script
          inputs:
            - content: echo "Hello from included bundle!"
      - activate-ssh-key: {}
    inputs:
      - bundle_input: default_value
        opts:
          title: Bundle Input
          summary: An input for the included bundle
  deploy-bundle:
    title: Deploy bundle
    summary: Deployment steps bundled together
    steps:
      - deploy-to-bitrise-io@1.7.1: {}
